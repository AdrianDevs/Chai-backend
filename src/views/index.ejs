<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mini Message Board</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <%- include('navbar', {links: links}) %>
    <header>
      <h1>Welcome to the Mini Message Board Project</h1>
    </header>
    <main>
      <section>
        <% if (locals.user) {%>
          <p>Welcom back 
            <strong><%= user.username %></strong>
          </p>
          <a href="user/logout">Logout</a>
        <% } else { %>
          <p>Have a look around and see whats happening.</p>
        <% } %>
      </section>
    </main>
  </body>
</html>
